{
  "name": "@inboxcenter/api",
  "version": "1.0.0",
  "private": true,
  "description": "Express API (MVC) — ระหว่างเปลี่ยนผ่าน server.js เดิมยังเรียกใช้ผ่าน shim ใน lib/",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -b tsconfig.json && node scripts/copy-assets.mjs",
    "typecheck": "tsc -b tsconfig.json --dry"
  },
  "devDependencies": {
    "@types/express": "^4.17.25",
    "@types/node": "^22.20.1",
    "@types/pg": "^8.20.0"
  },
  "dependencies": {
    "express": "^4.21.0",
    "pino": "^10.3.1",
    "pino-http": "^11.0.0",
    "zod": "^4.4.3"
  }
}
